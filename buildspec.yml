version: 0.2
phases:
  pre_build:
    commands:
       - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 cp --recursive --acl public-read dist/* s3://112demo/ --region "ap-south-1"
artifacts:
  files:
    - '**/*'
  base-directory: dist  